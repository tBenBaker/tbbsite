<!DOCTYPE HTML>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700&family=Roboto:wght@300;400;700&display=swap">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 40px;
            background-color: #0f2a44;
            color: #ffffff;
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        h1 {
            font-family: 'Merriweather', serif;
            font-size: 2.2em;
            font-weight: bold;
            margin-bottom: 30px;
            color: #ffffff;
        }

        h2 {
            font-family: 'Merriweather', serif;
            font-size: 1.5em;
            font-weight: bold;
            margin-top: 40px;
            margin-bottom: 20px;
            color: #ffffff;
        }

        p {
            font-size: 1.1em;
            margin-bottom: 15px;
            color: #ffffff;
        }

        a {
            color: #ffffff;
            text-decoration: underline;
        }

        a:hover {
            color: #cccccc;
        }

        strong {
            font-weight: bold;
            color: #ffffff;
        }

        /* Mobile responsiveness */
        @media (max-width: 768px) {
            body {
                padding: 20px 15px;
            }
            
            h1 {
                font-size: 1.8em;
            }
            
            h2 {
                font-size: 1.3em;
            }
            
            p {
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    {{ content | safe }}
</body>
</html>